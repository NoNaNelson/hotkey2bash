[Unit]
Description=hotkey listener daemon
After=network.target


[Service]
Type=simple
ExecStart=/usr/local/bin/hotkeys2bash 
User=${whoami}
Group=${whoami}
Restart=always 

[Install]
WantedBy=default.target
